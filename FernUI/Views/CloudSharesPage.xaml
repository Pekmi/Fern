<Page
    x:Class="FernUI.Views.CloudSharesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FernUI.Views"
    xmlns:models="using:FernUI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromVerticalOffset="100" />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- SECTION EN COURS (Optionnelle) -->
        <TextBlock x:Name="ActiveUploadTitle" Text="Téléversement actif" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0,0,0,12" Grid.Row="0" Visibility="Collapsed"/>
        <Border x:Name="ActiveUploadBorder" Grid.Row="1" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Margin="0,0,0,32" Visibility="Collapsed">
            <Grid>
                <!-- ... contenu de l'upload actif ... -->
            </Grid>
        </Border>

        <!-- HISTORIQUE -->
        <TextBlock Text="Clips sur le Cloud" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0,0,0,12" Grid.Row="2" VerticalAlignment="Top"/>
        <ListView x:Name="CloudSharesListView" Grid.Row="2" Margin="0,32,0,0" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:CloudClip">
                    <Grid Padding="0,12" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind DisplayName}" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind Link}" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="{x:Bind ExpirationText}" VerticalAlignment="Center" Margin="24,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" FontWeight="Medium"/>

                        <Button Grid.Column="2" Click="CopyLinkButton_Click" ToolTipService.ToolTip="Copier le lien">
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE8C8;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Copier"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="3" Click="DeleteClipButton_Click" ToolTipService.ToolTip="Supprimer le clip" Margin="8,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE74D;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Supprimer"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
