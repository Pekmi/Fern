<Page
    x:Class="FernUI.Views.CloudSharesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FernUI.Views"
    xmlns:models="using:FernUI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromVerticalOffset="100" />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Partages Cloud" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" Margin="0,0,0,32"/>

        <!-- SECTION EN COURS -->
        <TextBlock Text="Téléversement actif" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0,0,0,12" Grid.Row="1"/>
        <Border Grid.Row="2" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Margin="0,0,0,32">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <FontIcon Glyph="&#xE898;" FontSize="24" Margin="0,0,20,0" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlHighlightAccentBrush}"/>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Epic Win - Warzone.mp4" FontWeight="SemiBold"/>
                    <Grid Margin="0,8,0,0">
                        <ProgressBar Value="65" Height="4" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock Text="65% - 12 Mo restants" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Annuler" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </Grid>
        </Border>

        <!-- HISTORIQUE -->
        <TextBlock Text="Historique des partages" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0,0,0,12" Grid.Row="3" VerticalAlignment="Top"/>
        <ListView x:Name="CloudSharesListView" Grid.Row="3" Margin="0,32,0,0" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:CloudShareItem">
                    <Grid Padding="0,12" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <Border Background="{ThemeResource SystemControlBackgroundAccentBrush}" CornerRadius="4" Padding="6,1">
                                    <TextBlock Text="{x:Bind Game}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="{x:Bind Link}" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="{x:Bind Expiration}" VerticalAlignment="Center" Margin="24,0" Foreground="OrangeRed" FontSize="12" FontWeight="Medium"/>

                        <Button Grid.Column="2" Click="CopyLinkButton_Click" ToolTipService.ToolTip="Copier le lien">
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE8C8;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Copier"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
